apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets
  namespace: external-secrets
  # After creating the IAM role via scripts/create_iam_role_for_external_secrets.sh,
  # annotate this ServiceAccount with the role ARN, e.g.:
  # eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/external-secrets-role
