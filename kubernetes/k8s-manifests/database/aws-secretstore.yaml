apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-secret-store
  namespace: shopnow-ns
spec:
  controller: shopnow
  provider:
    aws:
      service: SecretsManager
      region: ap-south-1
      # Credentials are inherited from the External Secrets controller's IRSA role.
